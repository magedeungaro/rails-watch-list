<%= simple_form_for [ list, bookmark ] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie %>
  <div class="mt-3">
    <%= f.submit "Add bookmark", class: "btn btn-primary" %>
    <%= link_to 'Cancel', list_path(list), class: 'btn btn-outline-danger' %>
  </div>
<% end %>
