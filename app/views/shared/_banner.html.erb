<section class="home-page--banner" style="<%= prepend_image_gradient(@movies.all.sample.poster_url) unless @movies.empty? %>">
  <div class="container d-flex text-white w-80 justify-content-center align-items-center fw-bold fs-2">
    <p>Save any movie</p>
  </div>
</section>
